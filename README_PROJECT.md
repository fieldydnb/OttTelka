OttTelka Flutter (flattened package)
-----------------------------------
Instructions summary:
- Create files on GitHub using 'Add file -> Create new file' and use paths from README_UPLOAD_INSTRUCTIONS.md mapping.
- Example: create file `lib/main.dart` and paste the content from main.dart in this ZIP.
- After creating `pubspec.yaml` and `lib/main.dart` commit everything, Codemagic should detect Flutter project.
- If Codemagic requires, create android files under android/... using the android_*.txt contents.
