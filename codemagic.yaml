workflows:
  android-workflow:
    name: OttTelka Android Build
    instance_type: mac_mini_m1
    environment:
      flutter: 3.24.0
      xcode: 15.0
      java: 17
    scripts:
      - name: Clean and setup
        script: |
          rm -rf android build .dart_tool pubspec.lock
          flutter clean
          flutter pub get

      - name: Regenerate Android with new Gradle
        script: |
          flutter create -t app --org com.stvrtv --project-name mobile .

      - name: Build APK
        script: |
          flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk

    publishing:
      email:
        recipients:
          - fieldydnb@gmail.com
