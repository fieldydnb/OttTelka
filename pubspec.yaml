name: otttelka
description: OttTelka - simple DASH OTT player
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  better_player: ^0.0.80

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
